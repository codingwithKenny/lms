/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.pexels.com',"res.cloudinary.com"], // Add the external domain here
    },
  };
  
  export default nextConfig;
  